import styled from "@emotion/styled";

const TopRightButtonContainer = styled("div")(() => ({
    position: "absolute",
    top: "0px",
    right: "2px",
}));

export default TopRightButtonContainer;
